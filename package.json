{
  "name": "playalong-animation-catalog",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive animation catalog for PlayAlong TV game overlays",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
